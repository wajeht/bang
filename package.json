{
  "name": "bang",
  "version": "0.0.1",
  "private": "true",
  "description": "DuckDuckGo's !Bangs, but on steroids",
  "scripts": {
    "dev": "concurrently --kill-others --names \"GO,BROWSER-SYNC\" -c \"bgGreen.black,bgMagenta.black\" \"npm run dev:go\" \"npm run dev:browser-sync\"",
    "dev:go": "$(go env GOPATH)/bin/air -c ./.air.toml",
    "dev:browser-sync": "browser-sync start --proxy 'localhost:80' --files 'public/**/*.css, **/*.html, **/*.go' --no-notify --ignore 'node_modules'"
  },
  "keywords": [
    "bang"
  ],
  "author": "wajeht",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "concurrently": "^9.0.1"
  }
}
